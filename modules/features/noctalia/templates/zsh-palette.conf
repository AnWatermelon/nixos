# zsh vi-mode indicator palette — rendered by noctalia from the wallpaper
# palette. Mirrors lualine's mode colors (neovim lua/matugen.lua):
#   vicmd  -> primary_container        (lualine "normal" bg)
#   viins  -> primary                  (lualine "insert" bg)
#   visual -> secondary_container      (lualine "visual" bg)
#   fg     -> surface_container_lowest (lualine base01 fg)
VI_MODE_IND_NORMAL_BG='{{colors.primary_container.default.hex}}'
VI_MODE_IND_INSERT_BG='{{colors.primary.default.hex}}'
VI_MODE_IND_VISUAL_BG='{{colors.secondary_container.default.hex}}'
VI_MODE_IND_FG='{{colors.surface_container_lowest.default.hex}}'
